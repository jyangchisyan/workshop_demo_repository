def callFunction(){
    echo 'call function'
}

pipeline {
    agent any
    stages {
        stage('Call function Example') {
            steps{
                callFunction()
            }
        }
    }
}